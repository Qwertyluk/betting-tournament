@using BettingTournament.Components.Common
@using BettingTournament.Core
@using BettingTournament.Core.Services
@inject RoundService RoundService

<MudDialog>
    <DialogContent>
        <GameForm @bind-Game="Game" />
    </DialogContent>
    <DialogActions>
        <Button OnClick="Cancel">Cancel</Button>
        <Button OnClick="Submit">Add</Button>
    </DialogActions>
</MudDialog>
@code {
    private Game Game { get; set; } = new();

    [CascadingParameter]
    private MudDialogInstance MudDialog { get; set; } = default!;

    private void Submit()
    {
        RoundService.AddGame(Game.HomeTeam, Game.AwayTeam, Game.DateTime);

        MudDialog.Close(DialogResult.Ok(true));
    }

    private void Cancel() => MudDialog.Cancel();
}